#
# Copy this file to ~/.dotfiles.env (or set DOTFILES_ENV_FILE to its path)
# and adjust the values for your system. The file is ignored by git.
#

# Absolute path to this dotfiles repository (optional; auto-detected from repo location).
# If you uncomment this, place it BEFORE variables that use $DOTLOC below.
# DOTLOC="$HOME/path/to/dotfiles"

# Root directory where you keep personal projects (optional).
DEV_HOME="$HOME/Developer"

# Location of the whisper.cpp build directory (used by scripts/whisper_transcription.sh).
WHISPER_CPP_DIR="$DEV_HOME/source/whisper.cpp"

# Personal/Work Cursor profile directories (kept outside the repo to remain private).
# Note: $DOTLOC is auto-detected before this file is sourced, so it's safe to use here.
CURSOR_USER_DATA_DIR="$DOTLOC/cursor/profile"
CURSOR_EXTENSIONS_DIR="$CURSOR_USER_DATA_DIR/Extensions"

# Optional overrides for third-party CLIs.
CLAUDE_BINARY="$HOME/.claude/local/claude"

# Name of the Cursor app
CURSOR_APP_NAME="Cursor"
