#------------------------------------------------------------------------------
# Private aliases — gitignored, never committed
# Copy to zsh/.zsh_aliases and fill in your values
#------------------------------------------------------------------------------

# AWS (account-specific)
# alias awslogin="aws configure sso"
# alias awsd="aws --profile <your-profile>"

# Auto-enable debug mode in project directories
# claude() {
#   if [[ "$PWD" == "$DEV_HOME/myproject"* ]]; then
#     command claude --debug "$@"
#   else
#     command claude "$@"
#   fi
# }

# Location-specific overrides
# alias weather='curl -s v2.wttr.in/<your-city>'
